import { IsDisplayRoomPipe } from './is-display-room.pipe';

describe('IsDisplayRoomPipe', () => {
  it('create an instance', () => {
    const pipe = new IsDisplayRoomPipe();
    expect(pipe).toBeTruthy();
  });
});
